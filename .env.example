# Environment Variables for Homeschool Generator Frontend

# Cognito Authentication Configuration
# Cognito User Pool Domain (without the .auth.region.amazoncognito.com part)
VITE_COGNITO_DOMAIN=your-cognito-domain
# Example: VITE_COGNITO_DOMAIN=mabry-homeschool-dev

# Cognito User Pool App Client ID
VITE_COGNITO_CLIENT_ID=your-cognito-client-id
# Example: VITE_COGNITO_CLIENT_ID=1a2b3c4d5e6f7g8h9i0j1k2l3m

# AWS Region (optional - defaults to us-east-1)
VITE_AWS_REGION=us-east-1

# Redirect URI after successful login (optional - defaults to current origin)
VITE_COGNITO_REDIRECT_URI=http://localhost:3000
# For production: VITE_COGNITO_REDIRECT_URI=https://your-domain.com

# Alternative: If you're using Create React App instead of Vite, use REACT_APP_ prefix:
# REACT_APP_COGNITO_DOMAIN=your-cognito-domain
# REACT_APP_COGNITO_CLIENT_ID=your-cognito-client-id
# REACT_APP_AWS_REGION=us-east-1
# REACT_APP_COGNITO_REDIRECT_URI=http://localhost:3000

# API Configuration
# Main API Gateway URL for general endpoints
VITE_API_ENDPOINT=https://your-api-gateway.execute-api.us-east-1.amazonaws.com/prod
# Example: VITE_API_ENDPOINT=https://abc123def.execute-api.us-east-1.amazonaws.com/prod

# Specific feedback API endpoint (optional - falls back to VITE_API_ENDPOINT)
VITE_FEEDBACK_API_ENDPOINT=https://your-api-gateway.execute-api.us-east-1.amazonaws.com/prod
# Example: VITE_FEEDBACK_API_ENDPOINT=https://feedback123.execute-api.us-east-1.amazonaws.com/prod

# Alternative: If using Create React App:
# REACT_APP_API_ENDPOINT=https://your-api-gateway.execute-api.us-east-1.amazonaws.com/prod
# REACT_APP_FEEDBACK_API_ENDPOINT=https://your-api-gateway.execute-api.us-east-1.amazonaws.com/prod

# Note: Some variables appear duplicated because different parts of the codebase 
# use different naming conventions (react-oidc-context vs aws-amplify). 
# Set all variables below, using the same values where indicated.

# AWS Region
REACT_APP_AWS_REGION=us-east-1

# AWS Cognito Configuration
# User Pool ID (e.g., us-east-1_ABC123456)
REACT_APP_COGNITO_USER_POOL_ID=your-user-pool-id
REACT_APP_USER_POOL_ID=your-user-pool-id  # Same as above (used by Amplify)

# User Pool Client ID (App Client ID)
REACT_APP_COGNITO_USER_POOL_CLIENT_ID=your-client-id
REACT_APP_USER_POOL_WEB_CLIENT_ID=your-client-id  # Same as above (used by Amplify)

# OIDC Client ID (same as User Pool Client ID)
REACT_APP_COGNITO_CLIENT_ID=your-client-id

# Cognito Domain (e.g., https://your-domain.auth.us-east-1.amazoncognito.com)
REACT_APP_COGNITO_DOMAIN=https://your-user-pool-domain.auth.us-east-1.amazoncognito.com
REACT_APP_USER_POOL_DOMAIN=your-user-pool-domain.auth.us-east-1.amazoncognito.com  # Domain only (used by Amplify)

# OIDC Authority (same as Cognito Domain for OIDC authentication)
REACT_APP_COGNITO_AUTHORITY=https://your-user-pool-domain.auth.us-east-1.amazoncognito.com

# Logout redirect URI (where users are redirected after logout)
REACT_APP_LOGOUT_URI=https://your-domain.com

# OIDC Redirect URI (optional - defaults to window.location.origin if not set)
# REACT_APP_COGNITO_REDIRECT_URI=https://your-domain.com

# API Configuration (CRITICAL: Keep these endpoints private and use environment variables)
REACT_APP_API_ENDPOINT=https://your-api-gateway-url.amazonaws.com/prod
REACT_APP_API_GATEWAY_URL=https://your-api-gateway-url.amazonaws.com/prod  # Same value as REACT_APP_API_ENDPOINT

